name: Game Results Monitor

on:
  schedule:
    # Оптимизированное расписание по дням недели:
    # Будни (Пн-Пт): 18:00-01:00 каждые 15 минут (время игр)
    # Выходные (Сб-Вс): 11:00-01:00 каждые 15 минут (больше игр)
    # Понедельник-Пятница: 18:00-01:00 (дни 1-5)
    - cron: '*/15 18-23 * * 1-5'  # 18:00-23:59 Пн-Пт
    - cron: '0,15,30,45 0 * * 2-6'  # 00:00-00:45 Пн-Пт (следующий день)
    # Суббота-Воскресенье: 11:00-01:00 (дни 6,0)
    - cron: '*/15 11-23 * * 6,0'  # 11:00-23:59 Сб-Вс
    - cron: '0,15,30,45 0 * * 0,1'  # 00:00-00:45 Сб-Вс (следующий день)
  workflow_dispatch: # Позволяет запускать вручную для отладки

jobs:
  monitor-game-results:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-github.txt
        
    - name: Run game results monitor
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        echo "🏀 Запуск мониторинга результатов игр (версия 2)..."
        echo "=================================================="
        python run_game_results_monitor_v2.py
        echo "=================================================="
        echo "Завершение мониторинга"
        
    - name: Upload logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: game-results-monitor-logs
        path: |
          game_monitor_history.json
          *.log
