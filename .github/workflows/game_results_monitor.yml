name: Game Results Monitor

on:
  schedule:
    # Запускаем каждые 5 минут для проверки игр, которые должны начаться в ближайшие 5 минут
    - cron: '*/5 * * * *'
  workflow_dispatch: # Позволяет запускать вручную для отладки

jobs:
  monitor-game-results:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-github.txt
        
    - name: Run game results monitor
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        echo "🏀 Запуск мониторинга результатов игр..."
        echo "=================================================="
        python run_game_results_monitor.py
        echo "=================================================="
        echo "Завершение мониторинга"
        
    - name: Upload logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: game-results-monitor-logs
        path: |
          game_monitor_history.json
          *.log
