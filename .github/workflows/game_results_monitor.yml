name: Game Results Monitor

on:
  schedule:
    # Оптимизированное расписание:
    # - Каждые 30 минут с 00:00 до 18:00 (утро/день - редко)
    # - Каждые 15 минут с 18:00 до 23:59 (вечер - время игр)
    - cron: '0,30 0-17 * * *'  # 00:00, 00:30, 01:00, ..., 17:30
    - cron: '*/15 18-23 * * *' # 18:00, 18:15, 18:30, ..., 23:45
  workflow_dispatch: # Позволяет запускать вручную для отладки

jobs:
  monitor-game-results:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-github.txt
        
    - name: Run game results monitor
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        echo "🏀 Запуск мониторинга результатов игр..."
        echo "=================================================="
        python run_game_results_monitor.py
        echo "=================================================="
        echo "Завершение мониторинга"
        
    - name: Upload logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: game-results-monitor-logs
        path: |
          game_monitor_history.json
          *.log
