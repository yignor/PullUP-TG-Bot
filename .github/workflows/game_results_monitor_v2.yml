name: Game Results Monitor V2

on:
  schedule:
    # Проверяем результаты игр ежедневно в 23:00 MSK (20:00 UTC)
    # Это время выбрано, чтобы игры точно завершились
    - cron: '0 20 * * *'  # 20:00 UTC = 23:00 MSK
  workflow_dispatch: # Позволяет запускать вручную для отладки

jobs:
  monitor-game-results:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Restore game results cache
      uses: actions/cache@v3
      with:
        path: |
          game_results_history.json
          test_game_results_history.json
        key: game-results-${{ github.ref }}
        restore-keys: |
          game-results-
        
    - name: Check game results files
      run: |
        echo "🔍 ПРОВЕРКА ФАЙЛОВ РЕЗУЛЬТАТОВ ИГР"
        echo "=================================================="
        echo "📋 Проверяем наличие файлов:"
        ls -la *.json 2>/dev/null || echo "❌ JSON файлы не найдены"
        echo ""
        echo "📄 Содержимое game_results_history.json (если есть):"
        if [ -f "game_results_history.json" ]; then
          cat game_results_history.json
        else
          echo "❌ Файл game_results_history.json не найден"
        fi
        echo "=================================================="
        
    - name: Initialize game results files
      run: |
        echo "🔧 ИНИЦИАЛИЗАЦИЯ ФАЙЛОВ РЕЗУЛЬТАТОВ ИГР"
        echo "=================================================="
        # Создаем пустые файлы, если они не существуют
        if [ ! -f "game_results_history.json" ]; then
          echo "📝 Создаем пустой game_results_history.json"
          echo '{}' > game_results_history.json
        fi
        if [ ! -f "test_game_results_history.json" ]; then
          echo "📝 Создаем пустой test_game_results_history.json"
          echo '{}' > test_game_results_history.json
        fi
        echo "✅ Файлы результатов игр инициализированы"
        echo "=================================================="
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install python-telegram-bot gspread google-auth python-dotenv aiohttp beautifulsoup4
        
    - name: Create .env file
      run: |
        echo "BOT_TOKEN=${{ secrets.BOT_TOKEN }}" >> .env
        echo "CHAT_ID=${{ secrets.CHAT_ID }}" >> .env
        echo "ANNOUNCEMENTS_TOPIC_ID=${{ secrets.ANNOUNCEMENTS_TOPIC_ID }}" >> .env
        echo "GOOGLE_SHEETS_CREDENTIALS=${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}" >> .env
        echo "SPREADSHEET_ID=${{ secrets.SPREADSHEET_ID }}" >> .env
        
    - name: Run Game Results Monitor V2
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
        ANNOUNCEMENTS_TOPIC_ID: ${{ secrets.ANNOUNCEMENTS_TOPIC_ID }}
        GOOGLE_SHEETS_CREDENTIALS: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}
        SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
      run: |
        echo "🏀 Запуск системы мониторинга результатов игр V2..."
        echo "🕐 Время запуска: $(date)"
        echo "📅 День недели: $(date +%u)"
        echo "🕐 Час: $(date +%H)"
        
        # Проверяем переменные окружения
        if [ -z "$BOT_TOKEN" ]; then
          echo "❌ BOT_TOKEN не настроен"
          exit 1
        fi
        if [ -z "$CHAT_ID" ]; then
          echo "❌ CHAT_ID не настроен"
          exit 1
        fi
        
        echo "✅ Все переменные окружения настроены"
        echo "🔄 Запуск системы мониторинга результатов..."
        echo "=================================================="
        
        # Запускаем финальную систему мониторинга результатов
        echo "🆕 Запуск финальной системы мониторинга результатов..."
        python run_game_results_monitor_final.py
        echo "=================================================="
        echo "Завершение мониторинга результатов"
        
    - name: Save game results cache
      uses: actions/cache@v3
      if: always()
      with:
        path: |
          game_results_history.json
          test_game_results_history.json
        key: game-results-${{ github.ref }}
        
    - name: Handle errors
      if: failure()
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        echo "❌ GitHub Actions завершился с ошибкой"
        echo "Проверка доступности переменных для отправки уведомления об ошибке..."
        
        if [ -z "$BOT_TOKEN" ]; then
          echo "⚠️ BOT_TOKEN недоступен для отправки уведомления об ошибке"
          echo "Добавьте BOT_TOKEN в GitHub Secrets для получения уведомлений об ошибках"
        fi
        
        if [ -z "$CHAT_ID" ]; then
          echo "⚠️ CHAT_ID недоступен для отправки уведомления об ошибке"
          echo "Добавьте CHAT_ID в GitHub Secrets для получения уведомлений об ошибках"
        fi
        
        echo "Ошибки будут отправлены в основной чат через скрипт"
        
    - name: Upload logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: game-results-monitor-v2-logs
        path: |
          game_results_history.json
          test_game_results_history.json
          *.log
