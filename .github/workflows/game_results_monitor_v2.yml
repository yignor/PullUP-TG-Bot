name: Game Results Monitor V2

on:
  schedule:
    # Новое расписание проверок результатов игр (все времена указаны в UTC):
    # Будни (Пн-Пт): 16:30-21:30 UTC = 19:30-00:30 MSK каждые 15 минут
    # Выходные (Сб-Вс): 08:30-21:30 UTC = 11:30-00:30 MSK каждые 15 минут
    
    # Будни (Понедельник-Пятница): 16:30-20:59 UTC = 19:30-23:59 MSK
    - cron: '30,45 16 * * 1-5'  # 16:30, 16:45 UTC = 19:30, 19:45 MSK
    - cron: '0,15,30,45 17-20 * * 1-5'  # 17:00-20:59 UTC = 20:00-23:59 MSK каждые 15 минут
    
    # Будни (Понедельник-Пятница): 21:00-21:30 UTC = 00:00-00:30 MSK (следующий день)
    - cron: '0,15,30 21 * * 1-5'  # 21:00, 21:15, 21:30 UTC = 00:00, 00:15, 00:30 MSK (Вт-Сб)
    
    # Выходные (Суббота-Воскресенье): 08:30-20:59 UTC = 11:30-23:59 MSK
    - cron: '30,45 8 * * 6,0'  # 08:30, 08:45 UTC = 11:30, 11:45 MSK
    - cron: '0,15,30,45 9-20 * * 6,0'  # 09:00-20:59 UTC = 12:00-23:59 MSK каждые 15 минут
    
    # Выходные (Суббота-Воскресенье): 21:00-21:30 UTC = 00:00-00:30 MSK (следующий день)
    - cron: '0,15,30 21 * * 6,0'  # 21:00, 21:15, 21:30 UTC = 00:00, 00:15, 00:30 MSK (Вс-Пн)
    
  workflow_dispatch: # Позволяет запускать вручную для отладки

jobs:
  monitor-game-results:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Restore game results cache
      uses: actions/cache@v3
      with:
        path: |
          game_results_history.json
          test_game_results_history.json
        key: game-results-${{ github.ref }}
        restore-keys: |
          game-results-
        
    - name: Check game results files
      run: |
        echo "🔍 ПРОВЕРКА ФАЙЛОВ РЕЗУЛЬТАТОВ ИГР"
        echo "=================================================="
        echo "📋 Проверяем наличие файлов:"
        ls -la *.json 2>/dev/null || echo "❌ JSON файлы не найдены"
        echo ""
        echo "📄 Содержимое game_results_history.json (если есть):"
        if [ -f "game_results_history.json" ]; then
          cat game_results_history.json
        else
          echo "❌ Файл game_results_history.json не найден"
        fi
        echo "=================================================="
        
    - name: Initialize game results files
      run: |
        echo "🔧 ИНИЦИАЛИЗАЦИЯ ФАЙЛОВ РЕЗУЛЬТАТОВ ИГР"
        echo "=================================================="
        # Создаем пустые файлы, если они не существуют
        if [ ! -f "game_results_history.json" ]; then
          echo "📝 Создаем пустой game_results_history.json"
          echo '{}' > game_results_history.json
        fi
        if [ ! -f "test_game_results_history.json" ]; then
          echo "📝 Создаем пустой test_game_results_history.json"
          echo '{}' > test_game_results_history.json
        fi
        echo "✅ Файлы результатов игр инициализированы"
        echo "=================================================="
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-github.txt
        
    - name: Create .env file
      run: |
        echo "🔧 СОЗДАНИЕ ФАЙЛА .ENV"
        echo "=================================================="
        
        # Создаем пустой .env файл
        touch .env
        
        # Добавляем переменные только если они не пустые
        if [ ! -z "${{ secrets.BOT_TOKEN }}" ]; then
          echo "BOT_TOKEN=${{ secrets.BOT_TOKEN }}" >> .env
          echo "✅ BOT_TOKEN добавлен в .env"
        else
          echo "⚠️ BOT_TOKEN пустой, пропускаем"
        fi
        
        if [ ! -z "${{ secrets.CHAT_ID }}" ]; then
          echo "CHAT_ID=${{ secrets.CHAT_ID }}" >> .env
          echo "✅ CHAT_ID добавлен в .env"
        else
          echo "⚠️ CHAT_ID пустой, пропускаем"
        fi
        
        if [ ! -z "${{ secrets.ANNOUNCEMENTS_TOPIC_ID }}" ]; then
          echo "ANNOUNCEMENTS_TOPIC_ID=${{ secrets.ANNOUNCEMENTS_TOPIC_ID }}" >> .env
          echo "✅ ANNOUNCEMENTS_TOPIC_ID добавлен в .env"
        else
          echo "⚠️ ANNOUNCEMENTS_TOPIC_ID пустой, пропускаем"
        fi
        
        if [ ! -z "${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}" ]; then
          echo "GOOGLE_SHEETS_CREDENTIALS=${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}" >> .env
          echo "✅ GOOGLE_SHEETS_CREDENTIALS добавлен в .env"
        else
          echo "⚠️ GOOGLE_SHEETS_CREDENTIALS пустой, пропускаем"
        fi
        
        if [ ! -z "${{ secrets.SPREADSHEET_ID }}" ]; then
          echo "SPREADSHEET_ID=${{ secrets.SPREADSHEET_ID }}" >> .env
          echo "✅ SPREADSHEET_ID добавлен в .env"
        else
          echo "⚠️ SPREADSHEET_ID пустой, пропускаем"
        fi
        
        echo ""
        echo "📄 Содержимое .env файла:"
        cat .env
        echo "=================================================="
        
    - name: Run Game Results Monitor V2
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
        ANNOUNCEMENTS_TOPIC_ID: ${{ secrets.ANNOUNCEMENTS_TOPIC_ID }}
        GOOGLE_SHEETS_CREDENTIALS: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}
        SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
      run: |
        echo "🏀 Запуск системы мониторинга результатов игр V2..."
        echo "🕐 Время запуска: $(date)"
        echo "📅 День недели: $(date +%u)"
        echo "🕐 Час: $(date +%H)"
        
        # Проверяем переменные окружения
        if [ -z "$BOT_TOKEN" ]; then
          echo "❌ BOT_TOKEN не настроен"
          exit 1
        fi
        if [ -z "$CHAT_ID" ]; then
          echo "❌ CHAT_ID не настроен"
          exit 1
        fi
        
        echo "✅ Все переменные окружения настроены"
        
        # Проверяем наличие файлов
        echo "🔍 ПРОВЕРКА ФАЙЛОВ:"
        echo "=================================================="
        echo "📋 Текущая директория: $(pwd)"
        echo "📋 Содержимое директории:"
        ls -la
        
        echo ""
        echo "📋 Проверяем наличие Python файлов:"
        if [ -f "run_game_results_monitor_final.py" ]; then
          echo "✅ run_game_results_monitor_final.py найден"
          echo "📄 Содержимое файла:"
          head -10 run_game_results_monitor_final.py
        else
          echo "❌ run_game_results_monitor_final.py НЕ найден"
        fi
        
        if [ -f "game_results_monitor_final.py" ]; then
          echo "✅ game_results_monitor_final.py найден"
        else
          echo "❌ game_results_monitor_final.py НЕ найден"
        fi
        
        if [ -f "game_system_manager.py" ]; then
          echo "✅ game_system_manager.py найден"
        else
          echo "❌ game_system_manager.py НЕ найден"
        fi
        
        if [ -f "datetime_utils.py" ]; then
          echo "✅ datetime_utils.py найден"
        else
          echo "❌ datetime_utils.py НЕ найден"
        fi
        
        echo "=================================================="
        echo "🔄 Запуск системы мониторинга результатов..."
        echo "=================================================="
        
        # Проверяем .env файл перед запуском
        echo "🔍 ПРОВЕРКА .ENV ФАЙЛА:"
        echo "=================================================="
        if [ -f ".env" ]; then
          echo "✅ .env файл найден"
                  echo "📄 Содержимое .env файла:"
        cat .env
        echo ""
        echo "📋 Количество строк в .env:"
        wc -l .env
        echo ""
        echo "🔍 Проверка каждой строки .env файла:"
        if [ -f ".env" ]; then
          line_number=1
          while IFS= read -r line; do
            echo "   Строка $line_number: '$line'"
            line_number=$((line_number + 1))
          done < .env
        fi
        else
          echo "❌ .env файл не найден"
        fi
        echo "=================================================="
        
        # Запускаем финальную систему мониторинга результатов
        echo "🆕 Запуск финальной системы мониторинга результатов..."
        # При ручном запуске используем принудительный режим
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "🔧 Ручной запуск - используем принудительный режим"
          python run_game_results_monitor_final.py --force
        else
          echo "⏰ Автоматический запуск по расписанию"
          python run_game_results_monitor_final.py
        fi
        echo "=================================================="
        echo "Завершение мониторинга результатов"
        
    - name: Save game results cache
      uses: actions/cache@v3
      if: always()
      with:
        path: |
          game_results_history.json
          test_game_results_history.json
        key: game-results-${{ github.ref }}
        
    - name: Handle errors
      if: failure()
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        echo "❌ GitHub Actions завершился с ошибкой"
        echo "Проверка доступности переменных для отправки уведомления об ошибке..."
        
        if [ -z "$BOT_TOKEN" ]; then
          echo "⚠️ BOT_TOKEN недоступен для отправки уведомления об ошибке"
          echo "Добавьте BOT_TOKEN в GitHub Secrets для получения уведомлений об ошибках"
        fi
        
        if [ -z "$CHAT_ID" ]; then
          echo "⚠️ CHAT_ID недоступен для отправки уведомления об ошибке"
          echo "Добавьте CHAT_ID в GitHub Secrets для получения уведомлений об ошибках"
        fi
        
        echo "Ошибки будут отправлены в основной чат через скрипт"
        
    - name: Upload logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: game-results-monitor-v2-logs
        path: |
          game_results_history.json
          test_game_results_history.json
          *.log
