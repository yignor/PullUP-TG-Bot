name: PullUP Games Monitor

on:
  schedule:
    # Запуск каждые 30 минут
    - cron: '*/30 * * * *'
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-github.txt
        
    - name: Check secrets availability
      run: |
        echo "Checking secrets availability..."
        if [ -n "${{ secrets.BOT_TOKEN }}" ]; then
          echo "✅ BOT_TOKEN is available"
        else
          echo "⚠️ BOT_TOKEN is not available"
        fi
        
        if [ -n "${{ secrets.CHAT_ID }}" ]; then
          echo "✅ CHAT_ID is available"
        else
          echo "⚠️ CHAT_ID is not available"
        fi
        
        if [ -n "${{ secrets.TEST_CHAT_ID }}" ]; then
          echo "✅ TEST_CHAT_ID is available"
        else
          echo "⚠️ TEST_CHAT_ID is not available"
        fi
        
    - name: Run PullUP monitor
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
        TEST_CHAT_ID: ${{ secrets.TEST_CHAT_ID }}
      run: |
        # Функция для безопасной загрузки переменных из .env
        load_env_safely() {
          if [ -f .env ]; then
            echo "📁 .env файл найден, загружаем переменные..."
            # Загружаем только непустые строки без комментариев
            while IFS= read -r line; do
              # Пропускаем пустые строки и комментарии
              if [[ -n "$line" && ! "$line" =~ ^[[:space:]]*# ]]; then
                # Экспортируем переменную
                export "$line"
                echo "  ✅ Загружена: ${line%%=*}"
              fi
            done < .env
          else
            echo "❌ .env файл не найден"
            return 1
          fi
        }
        
        # Проверяем наличие необходимых переменных
        if [ -z "$BOT_TOKEN" ]; then
          echo "❌ BOT_TOKEN не установлен в secrets"
          if load_env_safely; then
            echo "✅ Переменные загружены из .env"
          else
            echo "❌ Не удалось загрузить переменные из .env"
            exit 1
          fi
        fi
        
        if [ -z "$CHAT_ID" ]; then
          echo "❌ CHAT_ID не установлен в secrets"
          if load_env_safely; then
            echo "✅ Переменные загружены из .env"
          else
            echo "❌ Не удалось загрузить переменные из .env"
            exit 1
          fi
        fi
        
        # Устанавливаем значение по умолчанию для TEST_CHAT_ID если оно не задано
        if [ -z "$TEST_CHAT_ID" ]; then
          echo "TEST_CHAT_ID не установлен, используем значение по умолчанию"
          export TEST_CHAT_ID="-15573582"
        fi
        
        # Проверяем финальные значения
        echo "BOT_TOKEN: ${BOT_TOKEN:0:10}..."
        echo "CHAT_ID: $CHAT_ID"
        echo "TEST_CHAT_ID: $TEST_CHAT_ID"
        
        # Проверяем наличие Python скрипта
        if [ ! -f "github_actions_monitor.py" ]; then
          echo "❌ Файл github_actions_monitor.py не найден"
          exit 1
        fi
        
        echo "Запуск монитора PullUP игр..."
        python github_actions_monitor.py
        
    - name: Handle errors
      if: failure()
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        TEST_CHAT_ID: ${{ secrets.TEST_CHAT_ID }}
      run: |
        echo "❌ GitHub Actions завершился с ошибкой"
        echo "Проверка доступности переменных для отправки уведомления об ошибке..."
        
        # Функция для безопасной загрузки переменных из .env
        load_env_safely() {
          if [ -f .env ]; then
            echo "📁 .env файл найден, загружаем переменные..."
            while IFS= read -r line; do
              if [[ -n "$line" && ! "$line" =~ ^[[:space:]]*# ]]; then
                export "$line"
                echo "  ✅ Загружена: ${line%%=*}"
              fi
            done < .env
          else
            echo "❌ .env файл не найден"
            return 1
          fi
        }
        
        if [ -z "$BOT_TOKEN" ]; then
          echo "⚠️ BOT_TOKEN недоступен для отправки уведомления об ошибке"
          if load_env_safely; then
            echo "✅ Переменные загружены из .env для отправки уведомления"
          fi
        fi
        
        # Устанавливаем значение по умолчанию для TEST_CHAT_ID если оно не задано
        if [ -z "$TEST_CHAT_ID" ]; then
          export TEST_CHAT_ID="-15573582"
        fi
        
        echo "Ошибки будут отправлены в тестовый чат через скрипт"
