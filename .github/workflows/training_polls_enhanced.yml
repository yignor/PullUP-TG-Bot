name: Training Polls Management

on:
  schedule:
    # Воскресенье 07:00-07:45 UTC = 10:00-10:45 MSK - Создание опроса
    - cron: '0,15,30,45 7 * * 0'
    # Среда 07:00-07:45 UTC = 10:00-10:45 MSK - Сбор данных за вторник (финальный)
    - cron: '0,15,30,45 7 * * 3'
    # Пятница 07:00-07:45 UTC = 10:00-10:45 MSK - Сбор данных за четверг (финальный)
    - cron: '0,15,30,45 7 * * 5'
    # Суббота 07:00-07:45 UTC = 10:00-10:45 MSK - Сбор данных за пятницу (финальный)
    - cron: '0,15,30,45 7 * * 6'
  workflow_dispatch:
    inputs:
      action:
        description: 'Действие для выполнения'
        required: true
        default: 'check'
        type: choice
        options:
        - check
        - create_poll
        - collect_tuesday
        - collect_thursday
        - collect_friday

jobs:
  training-polls:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Restore training polls cache
      uses: actions/cache@v3
      with:
        path: |
          current_poll_info.json
          training_data_collection_log.json
          weekly_training_polls.json
          weekly_training_poll_*.json
        key: training-polls-${{ github.ref }}
        restore-keys: |
          training-polls-
        
    - name: Check training polls files
      run: |
        echo "🔍 ПРОВЕРКА ФАЙЛОВ ОПРОСОВ ТРЕНИРОВОК"
        echo "=================================================="
        echo "📋 Проверяем наличие файлов:"
        ls -la *.json 2>/dev/null || echo "❌ JSON файлы не найдены"
        echo ""
        echo "📄 Содержимое current_poll_info.json (если есть):"
        if [ -f "current_poll_info.json" ]; then
          cat current_poll_info.json
        else
          echo "❌ Файл current_poll_info.json не найден"
        fi
        echo ""
        echo "📄 Содержимое training_data_collection_log.json (если есть):"
        if [ -f "training_data_collection_log.json" ]; then
          cat training_data_collection_log.json
        else
          echo "❌ Файл training_data_collection_log.json не найден"
        fi
        echo "=================================================="
        
    - name: Initialize training polls files
      run: |
        echo "🔧 ИНИЦИАЛИЗАЦИЯ ФАЙЛОВ ОПРОСОВ ТРЕНИРОВОК"
        echo "=================================================="
        # Создаем пустые файлы, если они не существуют
        if [ ! -f "current_poll_info.json" ]; then
          echo "📝 Создаем пустой current_poll_info.json"
          echo '{}' > current_poll_info.json
        fi
        if [ ! -f "training_data_collection_log.json" ]; then
          echo "📝 Создаем пустой training_data_collection_log.json"
          echo '{"collections": []}' > training_data_collection_log.json
        fi
        if [ ! -f "weekly_training_polls.json" ]; then
          echo "📝 Создаем пустой weekly_training_polls.json"
          echo '{}' > weekly_training_polls.json
        fi
        echo "✅ Файлы опросов инициализированы"
        echo "=================================================="
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install python-telegram-bot gspread google-auth python-dotenv
        
    - name: Create .env file
      run: |
        echo "BOT_TOKEN=${{ secrets.BOT_TOKEN }}" >> .env
        echo "CHAT_ID=${{ secrets.CHAT_ID }}" >> .env
        echo "ANNOUNCEMENTS_TOPIC_ID=${{ secrets.ANNOUNCEMENTS_TOPIC_ID }}" >> .env
        echo "GOOGLE_SHEETS_CREDENTIALS=${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}" >> .env
        echo "SPREADSHEET_ID=${{ secrets.SPREADSHEET_ID }}" >> .env
        
    - name: Run Training Polls Management
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
        ANNOUNCEMENTS_TOPIC_ID: ${{ secrets.ANNOUNCEMENTS_TOPIC_ID }}
        GOOGLE_SHEETS_CREDENTIALS: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}
        SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
      run: |
        echo "🏀 Запуск системы управления опросами тренировок..."
        echo "🕐 Время запуска: $(date)"
        echo "📅 День недели: $(date +%u)"
        
        # Проверяем переменные окружения
        if [ -z "$BOT_TOKEN" ]; then
          echo "❌ BOT_TOKEN не настроен"
          exit 1
        fi
        if [ -z "$CHAT_ID" ]; then
          echo "❌ CHAT_ID не настроен"
          exit 1
        fi
        if [ -z "$GOOGLE_SHEETS_CREDENTIALS" ]; then
          echo "❌ GOOGLE_SHEETS_CREDENTIALS не настроен"
          exit 1
        fi
        if [ -z "$SPREADSHEET_ID" ]; then
          echo "❌ SPREADSHEET_ID не настроен"
          exit 1
        fi
        
        echo "✅ Все переменные окружения настроены"
        echo "🔄 Запуск системы управления опросами..."
        echo "=================================================="
        
        # Запускаем систему тренировочных опросов
        echo "🏀 Запуск системы тренировочных опросов..."
        python training_polls_enhanced.py
        
        echo "=================================================="
        echo "Завершение работы системы"
        
    - name: Check files before cache
      run: |
        echo "🔍 ПРОВЕРКА ФАЙЛОВ ПЕРЕД СОХРАНЕНИЕМ В CACHE"
        echo "=================================================="
        if [ -f "current_poll_info.json" ]; then
          echo "✅ current_poll_info.json существует"
        else
          echo "❌ current_poll_info.json не существует"
        fi
        if [ -f "training_data_collection_log.json" ]; then
          echo "✅ training_data_collection_log.json существует"
        else
          echo "❌ training_data_collection_log.json не существует"
        fi
        echo "=================================================="
        
    - name: Save training polls cache
      uses: actions/cache@v3
      if: always()
      with:
        path: |
          current_poll_info.json
          training_data_collection_log.json
          weekly_training_polls.json
          weekly_training_poll_*.json
        key: training-polls-${{ github.ref }}
        
    - name: Upload logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: training-polls-logs
        path: |
          *.json
          *.log
        retention-days: 7
