# 🚀 Развертывание бота для новой команды

Эта инструкция поможет вам быстро развернуть бота для другой команды.

## 📋 Варианты развертывания

### Вариант 1: Клонирование с GitHub (рекомендуется)

Если репозиторий уже на GitHub:

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/OWNER/REPO_NAME.git my-new-team-bot
cd my-new-team-bot

# 2. Удалите связь с оригинальным репозиторием
git remote remove origin

# 3. Создайте новый репозиторий на GitHub (опционально)
# Перейдите на https://github.com/new и создайте новый репозиторий

# 4. Подключите новый репозиторий
git remote add origin https://github.com/YOUR_USERNAME/YOUR_NEW_REPO.git

# 5. Отправьте код в новый репозиторий
git push -u origin master
```

### Вариант 2: Копирование локальной папки

Если вы хотите скопировать локальную папку:

```bash
# 1. Скопируйте всю папку
cp -r /path/to/basketball-bot /path/to/new-team-bot
cd /path/to/new-team-bot

# 2. Удалите git историю (опционально, если хотите начать с чистого листа)
rm -rf .git

# 3. Инициализируйте новый git репозиторий
git init
git add .
git commit -m "Initial commit for new team"

# 4. Создайте новый репозиторий на GitHub и подключите его
git remote add origin https://github.com/YOUR_USERNAME/YOUR_NEW_REPO.git
git push -u origin master
```

### Вариант 3: Форк репозитория (для публичного репозитория)

Если репозиторий публичный:

1. Перейдите на страницу оригинального репозитория на GitHub
2. Нажмите кнопку **Fork** в правом верхнем углу
3. Выберите свой аккаунт
4. Клонируйте свой форк:
   ```bash
   git clone https://github.com/YOUR_USERNAME/REPO_NAME.git
   cd REPO_NAME
   ```

---

## ⚙️ Настройка для новой команды

После клонирования/копирования нужно настроить бота для новой команды:

### 1. Создайте нового Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Создайте нового бота с уникальным именем
4. **Сохраните токен** — он понадобится позже

### 2. Создайте новую Google таблицу

1. Создайте новую Google таблицу на [Google Sheets](https://sheets.google.com)
2. Назовите её (например: "New Team Bot Data")
3. Скопируйте **ID таблицы** из URL

### 3. Настройте Google Cloud (или используйте существующий проект)

**Вариант A: Использовать существующий проект**
- Добавьте новый Service Account в существующий проект
- Или используйте существующий Service Account (если у него есть доступ к новой таблице)

**Вариант B: Создать новый проект**
- Создайте новый проект в Google Cloud Console
- Включите Google Sheets API
- Создайте Service Account
- Скачайте JSON ключ
- Предоставьте доступ Service Account к новой таблице

### 4. Настройте GitHub Secrets

В новом репозитории (или в вашем форке):

1. Перейдите в **Settings** → **Secrets and variables** → **Actions**
2. Добавьте все необходимые секреты:
   - `BOT_TOKEN` — токен нового бота
   - `CHAT_ID` — ID чата новой команды
   - `GAMES_TOPIC_ID` — ID топика для опросов (если используется)
   - `ANNOUNCEMENTS_TOPIC_ID` — ID топика для анонсов (если используется)
   - `GOOGLE_SHEETS_CREDENTIALS` — JSON с учетными данными Google
   - `SPREADSHEET_ID` — ID новой таблицы

### 5. Настройте Google таблицу

В новой Google таблице:

1. **Создайте лист "Сервисный"** (бот создаст заголовки автоматически при первом запуске)
2. **Создайте лист "Игроки"** со следующими колонками:
   - Фамилия
   - Имя
   - Дата рождения
   - Никнейм (опционально)
   - Telegram ID (опционально)
3. **Настройте конфигурацию в листе "Сервисный"**:
   - Добавьте строки `CONFIG_TEAM` с ID команд новой команды
   - Добавьте строки `CONFIG_COMP` с ID соревнований
   - Добавьте строки `TRAINING_POLL` для опросов тренировок (если нужно)

### 6. Удалите старые данные (если копировали локально)

Если вы копировали локальную папку, удалите старые данные:

```bash
# Удалите старые JSON файлы с данными
rm -f *.json
rm -f !(game_announcements.json|pyrightconfig.json)

# Удалите старый .env (если есть)
rm -f .env

# Удалите старые логи
rm -f *.log
```

### 7. Создайте новый .env файл (для локального тестирования)

```bash
cp env.example .env
```

Заполните `.env` новыми значениями:
- `BOT_TOKEN` — токен нового бота
- `CHAT_ID` — ID чата новой команды
- `SPREADSHEET_ID` — ID новой таблицы
- И т.д.

---

## ✅ Чеклист для новой команды

- [ ] Репозиторий склонирован/скопирован
- [ ] Создан новый Telegram бот
- [ ] Создана новая Google таблица
- [ ] Настроен Google Cloud (Service Account)
- [ ] Service Account имеет доступ к новой таблице
- [ ] Все GitHub Secrets добавлены с новыми значениями
- [ ] Лист "Сервисный" настроен с конфигурацией новой команды
- [ ] Лист "Игроки" создан и заполнен
- [ ] Старые данные удалены (если копировали локально)
- [ ] GitHub Actions протестированы
- [ ] Бот работает корректно

---

## 🔄 Обновление кода

Если вы используете форк или копию репозитория, и хотите получать обновления из оригинального репозитория:

### Для форка:

```bash
# Добавьте оригинальный репозиторий как upstream
git remote add upstream https://github.com/ORIGINAL_OWNER/REPO_NAME.git

# Получите обновления
git fetch upstream

# Смержите обновления в вашу ветку
git merge upstream/master
```

### Для копии:

```bash
# Добавьте оригинальный репозиторий как remote
git remote add upstream https://github.com/ORIGINAL_OWNER/REPO_NAME.git

# Получите обновления
git fetch upstream

# Смержите обновления
git merge upstream/master
```

---

## 📝 Важные замечания

1. **Каждая команда должна иметь:**
   - Свой Telegram бот
   - Свою Google таблицу
   - Свои GitHub Secrets (если используете GitHub Actions)

2. **Можно использовать один Google Cloud проект** для нескольких команд, но лучше создать отдельный Service Account для каждой команды (для безопасности).

3. **GitHub Actions** будут работать автоматически для каждой команды, если настроены правильные Secrets.

4. **Конфигурация команд** хранится в Google таблице, поэтому один и тот же код может работать с разными командами, просто меняя `SPREADSHEET_ID`.

---

## 🆘 Проблемы?

Если возникли проблемы:
1. Проверьте, что все Secrets правильно настроены
2. Убедитесь, что Service Account имеет доступ к таблице
3. Проверьте логи GitHub Actions
4. См. раздел "Решение проблем" в [SETUP.md](SETUP.md)

---

## 🎯 Добавление дополнительных команд

Если вам нужно настроить бота для еще одной команды, у вас есть несколько вариантов:

### Вариант 1: Отдельный репозиторий для каждой команды (рекомендуется)

**Преимущества:**
- Полная изоляция команд
- Независимое управление версиями
- Легко удалить команду без влияния на другие
- Каждая команда может иметь свои настройки GitHub Actions

**Как добавить:**
1. Следуйте инструкциям выше (Вариант 1 или 2)
2. Создайте новый репозиторий на GitHub
3. Настройте все Secrets для новой команды
4. Готово!

**Структура:**
```
team-bot-1/          # Команда 1
team-bot-2/          # Команда 2
team-bot-3/          # Команда 3
```

### Вариант 2: Один репозиторий с несколькими наборами Secrets

Если вы хотите управлять несколькими командами из одного репозитория:

**Ограничение:** GitHub Secrets привязаны к репозиторию, поэтому для каждой команды нужен отдельный репозиторий или организация.

**Альтернатива:** Используйте переменные окружения в коде или внешний сервис для хранения секретов.

### Вариант 3: Один Google Cloud проект для всех команд

**Преимущества:**
- Централизованное управление
- Один счет за API
- Легче отслеживать использование

**Как настроить:**

1. **Создайте отдельный Service Account для каждой команды:**
   ```
   Google Cloud Console → IAM & Admin → Service Accounts
   → Create Service Account
   → Название: "team-1-bot", "team-2-bot", и т.д.
   ```

2. **Создайте ключи для каждого Service Account:**
   - Для каждой команды скачайте отдельный JSON ключ
   - Сохраните их с понятными именами: `team-1-credentials.json`, `team-2-credentials.json`

3. **Предоставьте доступ к соответствующим таблицам:**
   - Service Account команды 1 → доступ к таблице команды 1
   - Service Account команды 2 → доступ к таблице команды 2
   - И т.д.

4. **В GitHub Secrets используйте соответствующие credentials:**
   - Репозиторий команды 1 → `GOOGLE_SHEETS_CREDENTIALS` = содержимое `team-1-credentials.json`
   - Репозиторий команды 2 → `GOOGLE_SHEETS_CREDENTIALS` = содержимое `team-2-credentials.json`

### Вариант 4: Быстрое клонирование через скрипт

Создайте скрипт для автоматизации процесса:

```bash
#!/bin/bash
# deploy_new_team.sh

TEAM_NAME=$1
if [ -z "$TEAM_NAME" ]; then
    echo "Использование: ./deploy_new_team.sh team-name"
    exit 1
fi

# Клонируем репозиторий
git clone https://github.com/OWNER/REPO_NAME.git "basketball-bot-${TEAM_NAME}"
cd "basketball-bot-${TEAM_NAME}"

# Удаляем связь с оригиналом
git remote remove origin

# Удаляем старые данные
rm -f *.json !(game_announcements.json|pyrightconfig.json)
rm -f .env *.log

echo "✅ Репозиторий для команды ${TEAM_NAME} готов!"
echo "📝 Следующие шаги:"
echo "   1. Создайте новый репозиторий на GitHub"
echo "   2. git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git"
echo "   3. Настройте GitHub Secrets"
echo "   4. Создайте Google таблицу и настройте её"
```

Сохраните как `deploy_new_team.sh`, сделайте исполняемым:
```bash
chmod +x deploy_new_team.sh
./deploy_new_team.sh team-name
```

---

## 📊 Управление множеством команд

### Рекомендуемая структура

```
basketball-bots/
├── team-1/
│   ├── .git/
│   ├── game_system_manager.py
│   └── ...
├── team-2/
│   ├── .git/
│   ├── game_system_manager.py
│   └── ...
└── team-3/
    ├── .git/
    ├── game_system_manager.py
    └── ...
```

### Обновление кода для всех команд

Если вы внесли изменения в основной репозиторий и хотите обновить все команды:

```bash
# Создайте скрипт update_all_teams.sh
#!/bin/bash

BASE_REPO="https://github.com/ORIGINAL_OWNER/REPO_NAME.git"

for team_dir in */; do
    if [ -d "$team_dir/.git" ]; then
        echo "🔄 Обновление $team_dir..."
        cd "$team_dir"
        git fetch upstream
        git merge upstream/master
        cd ..
    fi
done

echo "✅ Все команды обновлены!"
```

### Мониторинг всех команд

Создайте дашборд или таблицу для отслеживания:

| Команда | Репозиторий | Google Таблица | Статус | Последнее обновление |
|---------|-------------|----------------|--------|---------------------|
| Team 1  | repo-url    | spreadsheet-id | ✅     | 2024-01-15         |
| Team 2  | repo-url    | spreadsheet-id | ✅     | 2024-01-14         |
| Team 3  | repo-url    | spreadsheet-id | ⚠️     | 2024-01-13         |

---

## 🔄 Миграция существующей команды

Если у вас уже есть настроенный бот и вы хотите перенести его на новую структуру:

1. **Экспортируйте текущие настройки:**
   - Сохраните все GitHub Secrets
   - Сохраните ID Google таблицы
   - Сохраните конфигурацию из листа "Сервисный"

2. **Создайте новый репозиторий:**
   - Следуйте инструкциям выше

3. **Импортируйте настройки:**
   - Добавьте все Secrets в новый репозиторий
   - Используйте существующую Google таблицу (или создайте новую)

4. **Протестируйте:**
   - Запустите GitHub Actions вручную
   - Проверьте, что все работает

---

## 💡 Советы по масштабированию

1. **Используйте шаблоны:**
   - Создайте шаблонный репозиторий на GitHub
   - Используйте его для быстрого создания новых команд

2. **Автоматизируйте:**
   - Создайте скрипты для развертывания
   - Используйте GitHub Actions для автоматической настройки

3. **Документируйте:**
   - Ведите список всех команд
   - Записывайте особенности каждой команды

4. **Мониторинг:**
   - Настройте уведомления об ошибках
   - Отслеживайте использование API

---

**Готово!** 🎉 Теперь у вас есть отдельный экземпляр бота для новой команды!

**Нужно добавить еще команду?** Просто повторите процесс выше! 🚀

