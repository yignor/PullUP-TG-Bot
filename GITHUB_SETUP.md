# 🚀 Создание нового проекта на GitHub

Эта инструкция поможет вам создать новый репозиторий на GitHub для вашей команды.

## 📋 Пошаговая инструкция

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Заполните форму:
   - **Repository name**: `basketball-team-bot` (или любое другое имя)
   - **Description**: "Telegram бот для управления баскетбольной командой"
   - **Visibility**: 
     - ✅ **Public** - если хотите сделать проект открытым
     - ✅ **Private** - если хотите оставить приватным
   - **НЕ добавляйте** README, .gitignore или лицензию (они уже есть в проекте)
3. Нажмите **Create repository**

### Шаг 2: Подключите локальный репозиторий к GitHub

Откройте терминал в папке проекта и выполните:

```bash
# Перейдите в папку проекта
cd /Users/y/Downloads/Basketball-Team-Bot

# Проверьте текущий remote (если есть)
git remote -v

# Если есть старый remote, удалите его
git remote remove origin

# Добавьте новый remote (замените YOUR_USERNAME и YOUR_REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Проверьте, что remote добавлен
git remote -v
```

**Пример:**
```bash
git remote add origin https://github.com/username/basketball-team-bot.git
```

### Шаг 3: Отправьте код на GitHub

```bash
# Проверьте статус
git status

# Добавьте все файлы (если есть изменения)
git add .

# Создайте коммит (если есть изменения)
git commit -m "Initial commit"

# Отправьте код на GitHub
git push -u origin master
```

Если у вас используется ветка `main` вместо `master`:

```bash
git push -u origin main
```

### Шаг 4: Настройте GitHub Secrets

После того как код отправлен на GitHub:

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты:

#### Обязательные секреты:

- **BOT_TOKEN**
  - Значение: токен вашего Telegram бота от @BotFather
  
- **CHAT_ID**
  - Значение: ID вашего чата (отрицательное число для групп)
  - Как получить: добавьте бота [@userinfobot](https://t.me/userinfobot) в чат

- **GOOGLE_SHEETS_CREDENTIALS**
  - Значение: весь JSON файл из Google Cloud Console (Service Account)
  - Скопируйте весь файл целиком, включая фигурные скобки

- **SPREADSHEET_ID**
  - Значение: ID вашей Google таблицы
  - Находится в URL таблицы между `/d/` и `/edit`

#### Опциональные секреты:

- **GAMES_TOPIC_ID**
  - Значение: ID топика для опросов по играм (если используете форумы)

- **ANNOUNCEMENTS_TOPIC_ID**
  - Значение: ID топика для анонсов (если используете форумы)

- **TARGET_TEAMS** (устарело, можно не добавлять)

### Шаг 5: Включите GitHub Actions

1. В репозитории перейдите в **Settings** → **Actions** → **General**
2. Убедитесь, что выбрано:
   - ✅ **Allow all actions and reusable workflows**
3. Прокрутите вниз и нажмите **Save**

### Шаг 6: Протестируйте

1. Перейдите в **Actions** в вашем репозитории
2. Выберите workflow **Daily Operations**
3. Нажмите **Run workflow** → **Run workflow**
4. Дождитесь завершения выполнения
5. Проверьте логи на наличие ошибок

## 🔧 Настройка для SSH (опционально)

Если вы хотите использовать SSH вместо HTTPS:

```bash
# Удалите HTTPS remote
git remote remove origin

# Добавьте SSH remote
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO_NAME.git

# Проверьте подключение
ssh -T git@github.com

# Отправьте код
git push -u origin master
```

## 📝 Проверочный список

- [ ] Репозиторий создан на GitHub
- [ ] Локальный репозиторий подключен к GitHub
- [ ] Код отправлен на GitHub
- [ ] Все GitHub Secrets добавлены
- [ ] GitHub Actions включены
- [ ] Первый workflow запущен и выполнен успешно

## 🆘 Решение проблем

### Ошибка: "remote origin already exists"

```bash
# Удалите старый remote
git remote remove origin

# Добавьте новый
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
```

### Ошибка: "failed to push some refs"

```bash
# Получите изменения с GitHub (если есть)
git pull origin master --allow-unrelated-histories

# Или принудительно отправьте (осторожно!)
git push -u origin master --force
```

### Ошибка авторизации

Если GitHub требует авторизацию:

1. Используйте Personal Access Token вместо пароля
2. Создайте токен: **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. Используйте токен как пароль при push

## ✅ Готово!

После выполнения всех шагов ваш проект будет на GitHub и готов к работе!

---

**Следующие шаги:**
- Настройте Google таблицу (см. [SETUP.md](SETUP.md))
- Добавьте конфигурацию команд в лист "Сервисный"
- Заполните лист "Игроки" данными игроков

